gem 'jekyll-paginate'
source "https://rubygems.org"
gem 'github-pages'


gem 'nokogiri', '~> 1.10', :platforms => [:ruby, :mri], :require => 'nokogiri' 

# Add necessary flags if needed

# gem 'nokogiri', '~> 1.10', :platforms => [:ruby_interpreter, :mri], :require => 'nokogiri', '--with-xml2-lib=/path/to/libxml2', '--with-xml2-include=/path/to/libxml2/include' 

